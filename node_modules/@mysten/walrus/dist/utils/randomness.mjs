//#region src/utils/randomness.ts
function weightedShuffle(arr) {
	return arr.map(({ value, weight }) => ({
		value,
		weight: Math.pow(Math.random(), 1 / weight)
	})).sort((a, b) => b.weight - a.weight).map((item) => item.value);
}
function shuffle(arr) {
	const result = [...arr];
	for (let i = result.length - 1; i > 0; i -= 1) {
		const j = Math.floor(Math.random() * (i + 1));
		[result[i], result[j]] = [result[j], result[i]];
	}
	return result;
}

//#endregion
export { shuffle, weightedShuffle };
//# sourceMappingURL=randomness.mjs.map