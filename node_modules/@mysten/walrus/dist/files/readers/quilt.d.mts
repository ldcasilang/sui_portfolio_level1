import { QuiltFileReader } from "./quilt-file.mjs";
import { BlobReader } from "./blob.mjs";

//#region src/files/readers/quilt.d.ts
interface QuiltReaderOptions {
  blob: BlobReader;
}
declare class QuiltReader {
  #private;
  constructor({
    blob
  }: QuiltReaderOptions);
  getBlobHeader(sliverIndex: number): Promise<{
    identifier: string;
    tags: Record<string, string> | null;
    blobSize: number;
    contentOffset: number;
  }>;
  readBlob(sliverIndex: number): Promise<{
    identifier: string;
    tags: Record<string, string> | null;
    blobContents: Uint8Array<ArrayBuffer>;
  }>;
  readerForPatchId(id: string): QuiltFileReader;
  readIndex(): Promise<{
    identifier: string;
    patchId: string;
    tags: {
      [k: string]: string;
    };
    reader: QuiltFileReader;
  }[]>;
}
//#endregion
export { QuiltReader };
//# sourceMappingURL=quilt.d.mts.map