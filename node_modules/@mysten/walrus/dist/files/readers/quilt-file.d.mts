import { FileReader } from "../file.mjs";
import { QuiltReader } from "./quilt.mjs";

//#region src/files/readers/quilt-file.d.ts

declare class QuiltFileReader implements FileReader {
  #private;
  constructor({
    quilt,
    sliverIndex,
    identifier,
    tags
  }: {
    quilt: QuiltReader;
    sliverIndex: number;
    identifier?: string;
    tags?: Record<string, string>;
  });
  getBytes(): Promise<Uint8Array>;
  getIdentifier(): Promise<string>;
  getTags(): Promise<Record<string, string>>;
}
//#endregion
export { QuiltFileReader };
//# sourceMappingURL=quilt-file.d.mts.map